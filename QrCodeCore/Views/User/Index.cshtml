@model List<QrCodeCore.Models.SysUsers>
@{
    ViewBag.Title = "Index";
    int counter = 1;
}
<style>
    .results tr[visible='false'],
    .no-result {
        display: none;
    }

    .results tr[visible='true'] {
        display: table-row;
    }

    .counter {
        padding: 8px;
        color: #ccc;
    }
</style>
<div class="row">
    <div class="col-12">
        @if (ViewBag.countUser == 2)
        {
            <div class="row">
                <div class="col-2">
                    <button class="btn btn-primary" disabled data-toggle="modal" data-target="#Modal1"><i class="bi bi-plus me-1"></i>Bir Kullanıcı Ekle</button>

                </div>
                <div class="col-10">
                    <div class="alert alert-danger" role="alert" style="width: 200px">
                        Bir sistemde en fazla 2 kullanıcı açılabilir.
                    </div>
                </div>
            </div>
        }
        else
        {
            <button class="btn btn-primary" data-toggle="modal" data-target="#Modal1"><i class="bi bi-plus me-1"></i>Bir Kullanıcı Ekle</button>
        }
    </div>
</div>
<br />
<div class="modal fade" id="Modal1" tabindex="-1" role="dialog" aria-labelledby="Modal1Label" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title" id="Modal1Label">Bir Kullanıcı Ekleyin.</h2>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("AddUser", "User", FormMethod.Post))
                {
                    <div class="row">
                        <div class="col-6">
                            <input type="text" class="form-control" name="User_Name" required />
                        </div>
                        <div class="col-6">
                            <input type="text" class="form-control" name="User_LastName" required />
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <input type="text" class="form-control" name="User_UserName" required />
                        </div>
                        <div class="col-6">
                            <input type="text" class="form-control" name="Password" required />
                        </div>
                    </div>
                }
                <button class="btn btn-primary" type="submit">Kullanıcıyı Ekle</button>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Vazgeç</button>

            </div>
        </div>
    </div>
</div>




<div class="card">
    <div class="card-body">
        <h5 class="card-title">Kullanıcılarınız</h5>
        <!-- Table with stripped rows -->
        <div class="form-group pull-right">
            <input type="text" class="search form-control" placeholder="Aramak istediğiniz kelimeyi girin">
        </div>
        <span class="counter pull-right"></span>
        <table class="table table-hover table-bordered results">
            <thead>
                <tr>
                    <th>#</th>
                    <th class="col-md-5 col-xs-5">Ad-Soyad</th>
                    <th class="col-md-4 col-xs-4">Kullanıcı Adı</th>
                    <th class="col-md-3 col-xs-3">Durumu</th>
                </tr>
                <tr class="warning no-result">
                    <td colspan="4"><i class="fa fa-warning"></i>Hiçbir Sonuç Bulunamadı.</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@counter</th>
                        <td>@item.User_FullName</td>
                        <td>@item.User_UserName</td>
                        @if (item.User_Status)
                        {
                            <td>Aktif</td>
                        }
                        else
                        {
                            <td>Pasif</td>
                        }
                        <td><a href="/Menu/UpdateFoodTypeStatus/@item.User_Id" class="btn btn-secondary"><i class="bi bi-arrow-repeat"></i></a></td>

                        <td><button type="button" data-toggle="modal" data-target=@("#Modal2-" + item.User_Id) data-url="@Url.Action($"UpdateFoodType/{item.User_Id}")" class="btn btn-warning"><i class="bi bi-pencil"></i></button></td>


                    </tr>
                    @*@Html.Partial("_UpdateModal", item)*@
                    counter++;
                }
            </tbody>
        </table>
        <!-- End Table with stripped rows -->

    </div>

</div>
@section Scripts {
    <script>
        $(document).ready(function () {
            $(".search").keyup(function () {
                var searchTerm = $(".search").val();
                var listItem = $('.results tbody').children('tr');
                var searchSplit = searchTerm.replace(/ /g, "'):containsi('")

                $.extend($.expr[':'], {
                    'containsi': function (elem, i, match, array) {
                        return (elem.textContent || elem.innerText || '').toLowerCase().indexOf((match[3] || "").toLowerCase()) >= 0;
                    }
                });

                $(".results tbody tr").not(":containsi('" + searchSplit + "')").each(function (e) {
                    $(this).attr('visible', 'false');
                });

                $(".results tbody tr:containsi('" + searchSplit + "')").each(function (e) {
                    $(this).attr('visible', 'true');
                });

                var jobCount = $('.results tbody tr[visible="true"]').length;
                $('.counter').text(jobCount + ' item');

                if (jobCount == '0') { $('.no-result').show(); }
                else { $('.no-result').hide(); }
            });
        });</script>
}

